// Arquivo de entrada do app (ex.: React/Vue/vanilla)
// Ajuste conforme o framework que usar.

export default function App() {
  const el = document.createElement('div');
  el.textContent = 'Meu Projeto - App.js';
  return el;
}
